import { FilterPodcastsPipe } from './filter-podcasts.pipe';

describe('FilterPodcastsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPodcastsPipe();
    expect(pipe).toBeTruthy();
  });
});
